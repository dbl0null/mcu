'use strict';

var promise = require('bluebird'),
    transform = require('sdp-transform');

class SDP {

    static buildWebRTCAnswer(offer) {
        var answer = '';
        return answer;
    }
}

module.exports = SDP;
